const foto = document.getElementById('trigger-secreto');

// Aquí debe ir una condición de intencionalidad, no un simple click
foto.addEventListener('click', () => {
    window.location.href = 'juego.html';
});